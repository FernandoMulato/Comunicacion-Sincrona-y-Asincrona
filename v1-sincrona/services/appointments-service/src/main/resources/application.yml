server:
  port: 8082

spring:
  application:
    name: appointments-service

  datasource:
    url: jdbc:postgresql://localhost:5432/appointments_db
    username: medical_user
    password: ${DB_PASSWORD:medical123}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  jackson:
    serialization:
      write-dates-as-timestamps: false

# Users service URL for synchronous communication
users-service:
  url: http://localhost:8081/api/users

# Management
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

# Logging
logging:
  level:
    com.medical: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: WARN